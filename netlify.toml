[build]
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Redirect /api/gemini → serverless function
[[redirects]]
  from  = "/api/gemini"
  to    = "/.netlify/functions/gemini-proxy"
  status = 200
